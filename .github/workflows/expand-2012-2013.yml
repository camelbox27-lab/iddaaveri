name: "Expand 3: 2012-2013"

on:
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 350

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      - run: pip install -r requirements.txt
      - uses: browser-actions/setup-firefox@v1
        with:
          firefox-version: 'latest'
      - uses: browser-actions/setup-geckodriver@latest

      - name: Restore previous data
        uses: actions/download-artifact@v4
        with:
          name: iddaa-2012-2013
          path: output/
        continue-on-error: true

      - name: Run scraper
        run: |
          mkdir -p output
          python scraper_cli.py \
            --start 2012-08-01 \
            --end 2013-07-31 \
            --output output/iddaa_2012_2013.xlsx \
            --max-hours 5.5 \
            --resume

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: iddaa-2012-2013
          path: |
            output/iddaa_2012_2013.xlsx
            progress_iddaa_2012_2013.json
          retention-days: 90
          overwrite: true

      - name: Commit to repo
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -f output/iddaa_2012_2013.xlsx progress_iddaa_2012_2013.json || true
          git diff --staged --quiet || git commit -m "Expand 3: 2012-2013 $(date +'%Y-%m-%d %H:%M')"
          git push || true
